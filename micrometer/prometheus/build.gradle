plugins {
    id 'java'
}

group 'com.amairovi.micrometer'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile 'io.micrometer:micrometer-registry-prometheus:latest.release'
}
